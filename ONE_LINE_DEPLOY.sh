cd ~/idiotic-betting && if [ -z "$(git remote -v 2>/dev/null)" ]; then echo "📋 First, create GitHub repo at https://github.com/new (name: idiotic-betting), then run:"; echo "   git remote add origin https://github.com/YOUR_USERNAME/idiotic-betting.git && git branch -M main && git push -u origin main"; echo ""; echo "Then deploy:"; echo "   Option 1: Go to https://railway.app → New Project → Deploy from GitHub"; echo "   Option 2: railway login && railway init && railway up"; else git push origin main 2>&1 && echo "✅ Pushed! Deploy: https://railway.app → New Project → Deploy from GitHub" || echo "⚠️  Push failed. Check your remote: git remote -v"; fi
